name: Build CLI Tools

on:
  push:
    branches: [master]
    paths:
      - "cli/**"
      - ".github/workflows/build-cli.yml"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUSTC_WRAPPER: sccache
  SCCACHE_GHA_ENABLED: "true"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - name: Install sccache
        uses: mozilla-actions/sccache-action@v0.0.10
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: cli
      - name: Check formatting
        working-directory: cli
        run: cargo fmt --all --check
      - name: Run clippy
        working-directory: cli
        run: cargo clippy --all-targets -- -D warnings
      - name: Run tests
        working-directory: cli
        run: cargo test

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cfg:
          - target: x86_64-unknown-linux-gnu
          - target: x86_64-unknown-linux-musl
          - target: i686-unknown-linux-gnu
          - target: aarch64-unknown-linux-gnu
          - target: x86_64-pc-windows-gnu
          - target: x86_64-pc-windows-gnu
            win7: true
          - target: i686-pc-windows-gnu
          - target: x86_64-apple-darwin
          - target: aarch64-apple-darwin
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - name: Install sccache
        uses: mozilla-actions/sccache-action@v0.0.10
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: cli
      - name: Install zig
        uses: mlugg/setup-zig@v2
      - name: Install cargo-zigbuild
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-zigbuild
      - name: Add target
        run: rustup target add ${{ matrix.cfg.target }}
      - name: Build
        working-directory: cli
        env:
          WIN32_WINNT: ${{ matrix.cfg.win7 && '0x0601' || '' }}
        run: cargo zigbuild --release --target ${{ matrix.cfg.target }} -p audiobook-scanner -p audiobook-organizer -p audiobook-transcriber -p audiobook-splitter
      - uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.cfg.win7 && format('{0}-win7', matrix.cfg.target) || matrix.cfg.target }}
          path: |
            cli/target/${{ matrix.cfg.target }}/release/audiobook-*
          compression-level: 0
